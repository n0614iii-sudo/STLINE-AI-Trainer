# 📚 GitHubへのプッシュ方法 - 初心者向けガイド

## 🤔 用語の説明

### Git（ギット）とは？
- **バージョン管理システム**：コードの変更履歴を管理するツール
- 例：ファイルを編集した履歴を保存し、以前のバージョンに戻せる

### GitHub（ギットハブ）とは？
- **コードを保存するクラウドサービス**：GitHubのサーバーにコードを保存できる
- 例：Googleドライブのようなもの（コード専用）

### リポジトリ（Repository）とは？
- **プロジェクトのフォルダ**：コードやファイルをまとめて保存する場所
- 例：このプロジェクト全体が1つのリポジトリ

### プッシュ（Push）とは？
- **アップロードすること**：ローカルのコードをGitHubに送信すること
- 例：ローカルのファイルをGitHubのサーバーにアップロード

## 🚀 実際の手順

### ステップ1: GitHubアカウントを作成（まだの場合）

1. https://github.com/ にアクセス
2. 「Sign up」をクリック
3. ユーザー名、メールアドレス、パスワードを入力
4. アカウントを作成

### ステップ2: GitHubでリポジトリを作成

1. GitHubにログイン
2. 右上の「+」ボタンをクリック
3. 「New repository」を選択
4. リポジトリ名を入力（例：`STLINE-AI-Trainer`）
5. 「Public」または「Private」を選択
6. **「Initialize this repository with a README」のチェックを外す**（重要！）
7. 「Create repository」をクリック

### ステップ3: ローカルでGitを初期化

ターミナルで以下を実行：

```bash
cd /Users/hikarunejikane/Downloads/STLINE-AI-Trainer

# Gitを初期化（まだの場合）
git init

# すべてのファイルを追加
git add .

# 最初のコミット（変更を保存）
git commit -m "Initial commit: STLINE AI Trainer"
```

### ステップ4: GitHubに接続

GitHubで作成したリポジトリのURLをコピーします。

例：`https://github.com/your-username/STLINE-AI-Trainer.git`

ターミナルで：

```bash
# GitHubリポジトリを接続
git remote add origin https://github.com/your-username/STLINE-AI-Trainer.git

# メインブランチを設定
git branch -M main

# GitHubにプッシュ（アップロード）
git push -u origin main
```

### ステップ5: 認証

初回はGitHubの認証が必要です：

- **ユーザー名**：GitHubのユーザー名を入力
- **パスワード**：GitHubのパスワード（またはPersonal Access Token）を入力

## 📝 詳細な手順（コピー&ペースト用）

### 1. プロジェクトディレクトリに移動

```bash
cd /Users/hikarunejikane/Downloads/STLINE-AI-Trainer
```

### 2. Gitを初期化（初回のみ）

```bash
git init
```

### 3. .gitignoreファイルを作成（オプション）

`.env`ファイルなど、アップロードしたくないファイルを除外：

```bash
cat > .gitignore << 'EOF'
# 環境変数ファイル（APIキーを含む）
.env

# Python関連
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
venv/
env/
ENV/

# ログファイル
*.log
logs/

# データファイル
data/*.json

# IDE関連
.vscode/
.idea/
*.swp
*.swo

# OS関連
.DS_Store
Thumbs.db
EOF
```

### 4. ファイルを追加

```bash
git add .
```

### 5. コミット（変更を保存）

```bash
git commit -m "Initial commit: STLINE AI Trainer"
```

### 6. GitHubリポジトリを作成

1. https://github.com/new にアクセス
2. リポジトリ名：`STLINE-AI-Trainer`
3. 「Create repository」をクリック

### 7. GitHubに接続してプッシュ

```bash
# あなたのGitHubユーザー名に置き換えてください
git remote add origin https://github.com/YOUR-USERNAME/STLINE-AI-Trainer.git

# ブランチ名をmainに設定
git branch -M main

# プッシュ（アップロード）
git push -u origin main
```

## 🔐 認証について

### Personal Access Token（推奨）

パスワードの代わりにPersonal Access Tokenを使用：

1. GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
2. 「Generate new token」をクリック
3. スコープで「repo」にチェック
4. トークンを生成してコピー
5. プッシュ時にパスワードの代わりにこのトークンを使用

## ✅ 確認方法

プッシュが成功したら：

1. GitHubのリポジトリページを開く
2. ファイルが表示されていれば成功！

## 🔄 今後の更新方法

コードを変更したら：

```bash
# 変更を確認
git status

# 変更を追加
git add .

# コミット
git commit -m "変更内容の説明"

# プッシュ
git push
```

## 🎯 まとめ

1. **GitHubアカウントを作成**
2. **GitHubでリポジトリを作成**
3. **ローカルでGitを初期化**
4. **ファイルを追加してコミット**
5. **GitHubに接続してプッシュ**

これで、コードがGitHubに保存され、Railwayなどのサービスから使用できるようになります！

