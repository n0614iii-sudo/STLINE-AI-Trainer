# ⚠️ エラー解決方法

## 🔴 現在のエラー

```
remote: Invalid username or token. Password authentication is not supported for Git operations.
fatal: Authentication failed
```

## ✅ 解決方法

**パスワードではなく、Personal Access Tokenを入力する必要があります！**

### ステップ1: Personal Access Tokenを確認

1. GitHubにログイン
2. 右上のプロフィール画像 → **Settings**
3. 左メニューの一番下 → **Developer settings**
4. **Personal access tokens** → **Tokens (classic)**
5. 作成したトークンを確認（または新しく作成）

⚠️ **重要**: トークンは一度しか表示されません。もし見失った場合は、新しいトークンを作成してください。

### ステップ2: もう一度プッシュを実行

ターミナルで以下を実行：

```bash
git push -u origin main
```

### ステップ3: 認証情報を入力

#### 3-1. ユーザー名

```
Username for 'https://github.com': 
```

→ `n0614iii-sudo` と入力 → Enterキー

#### 3-2. Personal Access Token（重要！）

```
Password for 'https://n0614iii-sudo@github.com': 
```

⚠️ **ここが重要！**

- **パスワードではなく、Personal Access Tokenを入力します**
- Personal Access Tokenをコピー
- ターミナルに **Command + V** で貼り付け
- **Enterキー**を押す

**注意**: 貼り付けても画面には何も表示されません（セキュリティのため）。これは正常です。貼り付けたらEnterキーを押してください。

## 💡 Personal Access Tokenの見つけ方

もしトークンが見つからない場合、新しいトークンを作成：

1. GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
2. **Generate new token** → **Generate new token (classic)**
3. 設定:
   - **Note**: `STLINE-AI-Trainer`
   - **Expiration**: お好みで（90 days または No expiration）
   - **Select scopes**: **`repo`** にチェック（重要！）
4. **Generate token** をクリック
5. **トークンをコピー**（⚠️ この画面を閉じると二度と見れません！）

## 🎯 まとめ

1. **Personal Access Tokenを準備**（GitHubで確認または作成）
2. **`git push -u origin main` を実行**
3. **ユーザー名**: `n0614iii-sudo`
4. **パスワード**: Personal Access Token（パスワードではない！）

## ✅ 成功した場合

以下のようなメッセージが表示されます：

```
Enumerating objects: 55, done.
Counting objects: 100% (55/55), done.
...
Branch 'main' set up to track remote branch 'main' from 'origin'.
```

これで完了です！

